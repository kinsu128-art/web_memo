# 메모관리 앱 - Linux 쉘 스크립트 사용 가이드

Linux/Mac 환경에서 쉘 스크립트를 통해 Docker를 쉽게 관리할 수 있습니다.

---

## 📁 쉘 스크립트 목록

### 1️⃣ **start.sh** - 애플리케이션 시작
```bash
./start.sh
또는
bash start.sh
```

**기능:**
- Docker 컴포즈 시작
- 서비스 상태 확인
- 브라우저 자동 열기 (Linux: xdg-open, Mac: open)
- 자동 IP 감지

**사용 예:**
```bash
cd /path/to/docker_test
./start.sh
```

---

### 2️⃣ **stop.sh** - 애플리케이션 중지
```bash
./stop.sh
또는
bash stop.sh
```

**기능:**
- 모든 컨테이너 중지
- 포트 해제

**사용 예:**
```bash
./stop.sh
```

---

### 3️⃣ **status.sh** - 상태 확인
```bash
./status.sh
또는
bash status.sh
```

**기능:**
- 서비스 상태 표시
- 포트 정보 확인
- API 연결성 테스트

**출력 예:**
```
NAME            STATUS
memo_backend    Up
memo_db         Up (healthy)
memo_frontend   Up (healthy)

포트 정보:
frontend: 0.0.0.0:80 -> 80
backend: 0.0.0.0:3000 -> 3000
db: 0.0.0.0:3306 -> 3306
```

---

### 4️⃣ **logs.sh** - 로그 확인
```bash
./logs.sh
또는
bash logs.sh
```

**기능:**
- 서비스별 로그 선택 조회
- 실시간 로그 스트리밍

**옵션:**
```
선택하세요:

1. 전체 로그
2. 백엔드 로그
3. 프론트엔드 로그
4. 데이터베이스 로그
5. 종료

번호를 입력하세요 (1-5): 2
```

---

### 5️⃣ **rebuild.sh** - 재빌드
```bash
./rebuild.sh
또는
bash rebuild.sh
```

**기능:**
- 이미지 재빌드 (코드 변경 후)
- 캐시 비우기
- 서비스 재시작

**사용 시기:**
- HTML, CSS, JavaScript 파일 수정 후
- 백엔드 코드 변경 후
- 데이터베이스 스키마 변경 후

---

### 6️⃣ **cleanup.sh** - 완전 정리
```bash
./cleanup.sh
또는
bash cleanup.sh
```

**기능:**
- 모든 컨테이너 삭제
- 모든 이미지 삭제
- 모든 데이터 삭제 (주의!)

**⚠️ 경고:**
데이터베이스의 모든 메모가 삭제됩니다!

---

## 🚀 빠른 시작

### 첫 실행
```bash
# 1. 스크립트 디렉토리 이동
cd /path/to/docker_test

# 2. 실행 권한 부여 (최초 1회만)
chmod +x *.sh

# 3. 시작
./start.sh

# 4. 브라우저에서 접속
# http://localhost 또는 http://[IP주소]
```

### 일상적 사용
```bash
아침:  ./start.sh        (시작)
작업:  애플리케이션 사용
저녁:  ./stop.sh         (중지)
```

### 문제 발생 시
```bash
1. ./status.sh           (현재 상태 확인)
2. ./logs.sh             (오류 로그 확인)
3. ./rebuild.sh          (재빌드)
4. ./cleanup.sh && ./start.sh (완전 초기화)
```

---

## 📋 실행 방법

### 방법 1: 직접 실행
```bash
./start.sh
```

### 방법 2: bash로 실행
```bash
bash start.sh
```

### 방법 3: sh로 실행 (POSIX 호환)
```bash
sh start.sh
```

### 방법 4: 백그라운드 실행
```bash
nohup ./start.sh > start.log 2>&1 &
```

---

## 🔧 커스터마이징

### 실행 권한 설정

처음 사용할 때 실행 권한을 줘야 합니다:

```bash
# 단일 파일에 권한 부여
chmod +x start.sh

# 모든 쉘 스크립트에 권한 부여
chmod +x *.sh
```

### 브라우저 자동 열기 비활성화

`start.sh` 파일 편집:
```bash
# 이 부분을 주석 처리
# if command -v xdg-open &> /dev/null; then
#     xdg-open "http://$IP" 2>/dev/null &
# elif command -v open &> /dev/null; then
#     open "http://$IP" 2>/dev/null &
# fi
```

### 자동으로 로그 파일에 저장

```bash
./start.sh > start.log 2>&1 &
```

---

## 💡 팁

### 1. cron에 등록하여 자동 시작

`crontab -e`에 추가:
```bash
# 매일 아침 8시에 시작
0 8 * * * cd /path/to/docker_test && ./start.sh >> /tmp/docker_start.log 2>&1

# 매일 저녁 6시에 중지
0 18 * * * cd /path/to/docker_test && ./stop.sh >> /tmp/docker_stop.log 2>&1
```

### 2. 로그 확인
```bash
./logs.sh 2>&1 | tee logs.txt
```

### 3. 상태 주기적 확인
```bash
watch -n 5 'docker compose ps'
```

### 4. 여러 명령 연결
```bash
./stop.sh && ./cleanup.sh && ./start.sh
```

---

## 🐛 트러블슈팅

### Q: "Permission denied" 에러
**A:** 실행 권한이 없음
```bash
chmod +x start.sh
```

### Q: "command not found: docker"
**A:** Docker가 설치되지 않았거나 PATH에 없음
- Docker 설치 확인
- PATH 확인: `echo $PATH`
- 재부팅

### Q: "Cannot connect to Docker daemon"
**A:** Docker 데몬이 실행 중이 아님
```bash
# Linux
sudo systemctl start docker

# Mac
open /Applications/Docker.app
```

### Q: "Port 3000 already in use"
**A:** 다른 프로세스가 포트 사용 중
```bash
# 포트 사용 프로세스 확인
lsof -i :3000

# 또는
netstat -tlnp | grep 3000

# 모든 서비스 중지
./stop.sh
```

### Q: 데이터가 날아갔습니다
**A:** `cleanup.sh` 때문일 수 있음
- 백업이 있으면 복구
- 이후 더 신중하게 사용

### Q: 스크립트가 실행 안 됨
**A:** shebang 확인
```bash
# 파일의 첫 줄이 다음이어야 함:
#!/bin/bash

# 확인 명령:
head -1 start.sh
```

---

## 🔐 보안 참고사항

### 스크립트 실행 전 검토
```bash
# 스크립트 내용 확인
cat start.sh

# 또는
less start.sh
```

### 수정 후 테스트
코드 변경 후에는 `status.sh`로 서비스 상태 확인:
```bash
./status.sh
```

---

## 📊 Linux/Mac 호환성

| 기능 | Linux | Mac |
|------|-------|-----|
| 실행 | ✅ | ✅ |
| 브라우저 자동 열기 | xdg-open | open |
| curl | ✅ | ✅ |
| grep | ✅ | ✅ |
| sed | ✅ | ✅ |

---

## 📞 도움말

### 각 스크립트의 역할
```
start.sh    - 시작 및 브라우저 열기
stop.sh     - 중지
status.sh   - 상태 확인
logs.sh     - 로그 조회
rebuild.sh  - 재빌드 (코드 변경 후)
cleanup.sh  - 완전 정리 (데이터 초기화)
```

### 빠른 명령 참조
```bash
./start.sh      # 시작
./stop.sh       # 중지
./status.sh     # 상태 확인
./logs.sh       # 로그 보기
./rebuild.sh    # 재빌드
./cleanup.sh    # 초기화
```

### Docker 명령 (직접 사용 시)
```bash
docker compose up -d          # 시작
docker compose down           # 중지
docker compose ps             # 상태
docker compose logs -f        # 로그
docker compose build          # 빌드
docker compose down -v        # 중지 + 볼륨 삭제
```

---

**Happy coding! 🚀**
